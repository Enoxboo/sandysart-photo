# Secrets — ne doivent jamais entrer dans l'image
.env
.env.*

# Dépendances (réinstallées dans l'image via npm ci)
node_modules

# Données locales / générées, pas destinées à l'image
photos.db
uploads/
*.tar.gz

# Outils de dev
debug_uploads.js
scripts/

# Git / éditeurs
.git/
.gitignore
.idea/
.vscode/

# Logs
*.log
